[phases.setup]
nixPkgs = ["python311", "pango", "fontconfig", "cairo", "gdk-pixbuf", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -c 'from app import init_db; init_db()' && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
